import { Box } from "@mui/material";
import { GridRenderCellParams } from "@mui/x-data-grid";
import React from "react";

export default function MediaSelect(params: GridRenderCellParams) {
  const { value } = params;
  return <Box></Box>;
}
